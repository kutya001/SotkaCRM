# Отчет о выполнении: Синхронизация с API api.sotka.kg, устранение ошибки веб-манифеста и перевод аутентификации строго на логины

**Дата и время:** 19-09-2026 01:53:00  
**Статус:** Успешно завершено (Verified)  

---

## 1. Выполненные задачи и архитектурные изменения

### 1.1. Устранение ошибки веб-манифеста PWA
- **Проблема:** Браузер запрашивал `/manifest.json`, но из-за отсутствия физического файла Next.js отдавал HTML-страницу 404, что вызывало синтаксическую ошибку парсинга (`manifest.json:1 Manifest: Line: 1, column: 1, Syntax error`).
- **Решение:**
  - Создан статический файл манифеста [`public/manifest.json`](file:///d:/Рабочий%20стол/Code%20Projects/SotkaCRM/public/manifest.json) согласно ТЗ.
  - Создан динамический генератор метаданных [`app/manifest.ts`](file:///d:/Рабочий%20стол/Code%20Projects/SotkaCRM/app/manifest.ts) (Next.js Metadata Route).
  - Добавлена векторная контурная иконка [`public/icon.svg`](file:///d:/Рабочий%20стол/Code%20Projects/SotkaCRM/public/icon.svg) в стиле Apple Island.
  - В роутинге Next.js зарегистрирован маршрут `○ /manifest.webmanifest`. Ошибка 404 и сбой парсера полностью устранены.

### 1.2. Перевод аутентификации и регистрации строго на логины (`login`)
- **Миграция БД ([`supabase/migrations/002_auth_by_login.sql`](file:///d:/Рабочий%20стол/Code%20Projects/SotkaCRM/supabase/migrations/002_auth_by_login.sql)):**
  - Полю `public.users.login` присвоено ограничение `NOT NULL` и создан регистронезависимый уникальный индекс `users_login_lower_idx`.
  - Создана вспомогательная функция `public.get_synthetic_email(login)`, формирующая синтетический адрес `${login.toLowerCase()}@internal.sotka.kg`, скрытый от пользователя.
  - Создана хранимая процедура `public.create_crm_user(...)` со статусом `SECURITY DEFINER` и валидацией прав `admin`. Процедура атомарно создает пользователя в `auth.users`, `auth.identities` и `public.users` без внешнего запроса email.
  - Существующие тестовые учетные записи переведены на синтетический домен `@internal.sotka.kg`:
    * `admin` -> `admin@internal.sotka.kg` (пароль `admin123456`, валиден)
    * `consultant1` -> `consultant1@internal.sotka.kg` (пароль `consultant123456`, валиден)
    * `smm_operator` -> `smm_operator@internal.sotka.kg` (пароль `smm123456`, валиден)
  - Миграция успешно применена к живой базе данных Supabase.
- **Интерфейс авторизации ([`app/login/page.tsx`](file:///d:/Рабочий%20стол/Code%20Projects/SotkaCRM/app/login/page.tsx)):**
  - Лейбл поля изменен строго на **«Логин»**.
  - Плейсхолдер: `admin, consultant1, smm_operator` (любые упоминания почты исключены).
- **Server Actions ([`app/auth/actions.ts`](file:///d:/Рабочий%20стол/Code%20Projects/SotkaCRM/app/auth/actions.ts)):**
  - Метод `login`: принимает `login` и `password`, выполняет поиск в `users` по логину, проверяет `is_active = true`, формирует синтетический email и осуществляет вход через Supabase Auth.
  - Метод `createCrmUser`: доступен администратору, создает сотрудника с указанием логина, пароля, ФИО, телефона и роли (`admin`, `consultant`, `smm`).

### 1.3. Серверная синхронизация с внешним API `api.sotka.kg`
- **Сервисный слой ([`lib/services/sotka-api.ts`](file:///d:/Рабочий%20стол/Code%20Projects/SotkaCRM/lib/services/sotka-api.ts)):**
  - `authenticateSotkaAdmin()`: POST на `/api/public/v1/auth/pair`, учетные данные 9-значного телефона, Bearer-токен.
  - `logoutSotkaSession(token)`: POST на `/api/public/v1/auth/logout`.
  - `fetchSellersOverview(token, offset, limit)`: постраничная выборка продавцов по 100 записей.
  - `fetchTransactions(token, offset, limit)`: выборка транзакций с fallback на `/api/private/v1/subscriptions/billing/transactions/`.
- **Серверный маршрут ([`app/api/sync/sotka/route.ts`](file:///d:/Рабочий%20стол/Code%20Projects/SotkaCRM/app/api/sync/sotka/route.ts)):**
  - Доступ строго для роли `admin` (403 для остальных).
  - Инвариант: считывание и сохранение привязок `manager_id` перед `upsert` в `sellers`.
  - Нормализация телефонов до `996XXXXXXXXX`.
  - Пакетная вставка в `sellers` и `payments` порциями по 100 записей.
  - Гарантированный `logoutSotkaSession` в `finally`.
  - Логирование времени исполнения (`durationMs`), количества обработанных записей и ошибок.
- **Интерфейс запуска ([`TopHeader.tsx`](file:///d:/Рабочий%20стол/Code%20Projects/SotkaCRM/components/layout/TopHeader.tsx) & [`AppLayout.tsx`](file:///d:/Рабочий%20стол/Code%20Projects/SotkaCRM/components/layout/AppLayout.tsx)):**
  - Кнопка синхронизации только для администратора со спиннером `RefreshCw`.
  - Фиксация времени последней синхронизации в `localStorage`.
  - Уведомления через матовый тост `Toast`.

### 1.4. Реестр продавцов ([`app/sellers/page.tsx`](file:///d:/Рабочий%20стол/Code%20Projects/SotkaCRM/app/sellers/page.tsx))
- Интеграция `DataJournal` (таблица на ПК, карточки на мобильных устройствах).
- Быстрые действия: 1-click звонок (`tel:`) и чат в WhatsApp (`wa.me`).
- Карточка продавца `EntityModal` с возможностью назначения куратора администратором.
- Ограничение доступа: доступен `admin` и `consultant`, авторедирект на `/leads` для `smm`.

### 1.5. Актуализация документации
- Документация базы данных [`DB.md`](file:///d:/Рабочий%20стол/Code%20Projects/SotkaCRM/DB.md) обновлена:
  - Спецификация таблицы `users` (инкапсуляция логинов, домен `@internal.sotka.kg`).
  - Индекс `users_login_lower_idx` в разделе 4.
  - Сигнатуры процедур `get_synthetic_email` и `create_crm_user` в подразделе 5.4.
- [`supabase/seed.sql`](file:///d:/Рабочий%20стол/Code%20Projects/SotkaCRM/supabase/seed.sql) обновлен с явной привязкой `auth_id`.

---

## 2. Результаты верификации

1. **База данных Supabase:**
   - Миграция `002_auth_by_login.sql` успешно применена.
   - Тестовые аккаунты переведены на `@internal.sotka.kg`, соответствие паролей проверено через `crypt()`:
     * `admin`: `is_admin_valid = true`
     * `consultant1`: `is_consultant_valid = true`
     * `smm_operator`: `is_smm_valid = true`
2. **Компиляция TypeScript:**
   - `npx tsc --noEmit` -> 0 ошибок (exit code 0).
3. **Продакшн сборка Next.js:**
   - `npm run build` -> 0 ошибок (exit code 0), сгенерированы все 9 маршрутов:
     * `○ /manifest.webmanifest` (127 B)
     * `ƒ /api/sync/sotka` (127 B)
     * `○ /login` (4.59 kB)
     * `○ /sellers` (5.07 kB)
     * `○ /leads` (9.58 kB)
     * `○ /` (3.19 kB)
